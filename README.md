How to run the application:

1. Start Docker Desktop.
2. Open a terminal and navigate to the directory containing the 'docker-compose.yml' file.
3. Run the command 'docker-compose up --build' in the terminal to begin building the project.
4. Open a web browser and connect host 'https://localhost'
